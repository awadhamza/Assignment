#ifndef REDIRECT_HH
#define REDIRECT_HH

#include <vector>
#include <iostream>

using std::cout;
using std::cin;
using std::string;
using std::vector;

class Redirect : public Base {
	public:
		Redirect() : Base() {}
		~Redirect();
		void execute(;
		bool is_Group();
		
};

#endif
