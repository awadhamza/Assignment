#ifndef EXECUTE_HH
#define EXECUTE_HH

#include <vector>
using std::vector;

class Execute {
	private:
	
	public:
		Execute();
		~Execute();
		void execute(vector<vector<char*>> , vector<int> );
		
};


#endif
